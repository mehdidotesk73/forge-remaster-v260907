[project]
name = "forge"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0,<3.0",
    "psycopg2-binary>=2.9,<3.0",
    "pytest>=7.4,<8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = "."